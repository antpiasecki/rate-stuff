generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Movie {
  id          String  @id @default(cuid())
  title       String
  year        Int
  imdb_image  String?
  imdb_rating Float?
  user_rating Int?

  @@unique([title, year])
}
